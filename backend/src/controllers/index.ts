//Here we are exporting all the controllers from the index file so that we can import all the controllers from a single file.
// In the routes file, you can import the controller that you need. 

export * as CompanyController from './company.controller';
export * as UserController from './user.controller';
export * as JobController from './job.controller';